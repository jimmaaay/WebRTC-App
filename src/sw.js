// Disable eslint as using self in the service worker and it doesn't like it
/* eslint-disable */

// // Cache resources in here
// if (process.env.NODE_ENV === 'production') {

// }


// self.addEventListener('install', (event) => {
//   event.waitUntil()
// });

