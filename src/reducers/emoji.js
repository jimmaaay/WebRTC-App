const defaultState = {
  open: false,
  recent: [], // last 10 recently used items
};


const emoji = (state = defaultState, action) => {

  switch (action.type) {

    default:
      return state;

  }

}


export default emoji;